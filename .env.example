# Ethics Jurisprudence Engine (EJE) - Environment Variables
# Copy this file to .env and fill in your actual API keys

# OpenAI API Key
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API Key (for Claude)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Google Gemini API Key
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

#=============================================================================
# SECURITY CONFIGURATION
#=============================================================================

# EJC Audit Signing Key (Gap #7: Cryptographic Audit Log Security)
# This key is used to generate HMAC-SHA256 signatures for audit log entries
# providing tamper detection and non-repudiation.
#
# CRITICAL SECURITY REQUIREMENTS:
# 1. Generate with: openssl rand -hex 32
# 2. Keep SECRET - never commit to version control!
# 3. Store securely (environment variable, secrets manager, vault)
# 4. Rotate periodically (every 90 days recommended)
#
# Example generation:
#   openssl rand -hex 32
#   Output: a1b2c3d4e5f6...
#
EJC_AUDIT_SIGNING_KEY=generate-your-own-secure-random-key-here-use-openssl-rand-hex-32

# Key version for rotation tracking (increment when rotating keys)
# EJC_AUDIT_KEY_VERSION=v1

#=============================================================================
# DATABASE CONFIGURATION
#=============================================================================

# PostgreSQL connection string (for production with Docker)
DATABASE_URL=postgresql://eje_user:change_me_in_production@postgres:5432/eleanor

# PostgreSQL password (used by docker-compose)
POSTGRES_PASSWORD=change_me_in_production

# Alternative: EJC_DB_URI (can be SQLite for local development)
# EJC_DB_URI=sqlite:///eleanor_data/eje.db

#=============================================================================
# APPLICATION CONFIGURATION
#=============================================================================

# Configuration file path
EJE_CONFIG_PATH=/app/config/global.yaml

# Data directory path
EJE_DATA_PATH=/app/eleanor_data

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

#=============================================================================
# PERFORMANCE TUNING
#=============================================================================

# Maximum number of parallel critic evaluations
MAX_PARALLEL_CALLS=5

# Enable result caching
ENABLE_CACHE=true

# Cache size (number of decisions to cache)
CACHE_SIZE=1000

# Critic timeout in seconds
CRITIC_TIMEOUT=30

#=============================================================================
# PRECEDENT SYSTEM CONFIGURATION
#=============================================================================

# Enable precedent system
PRECEDENT_ENABLED=true

# Precedent storage path
PRECEDENT_STORE_PATH=/app/data/precedents

# Embedding model for precedent similarity
PRECEDENT_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Note: Never commit your .env file to version control
# The .env file should be added to .gitignore
