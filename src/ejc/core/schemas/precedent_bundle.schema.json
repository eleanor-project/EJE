{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://eleanorproject.org/schemas/precedent_bundle.schema.json",
  "title": "Precedent Bundle",
  "description": "A stored precedent case with its hash for similarity matching",
  "type": "object",
  "required": ["case_hash", "request_id", "timestamp", "input", "final_decision"],
  "properties": {
    "case_hash": {
      "type": "string",
      "description": "SHA-256 hash of the input case for similarity matching",
      "pattern": "^[a-f0-9]{64}$"
    },
    "request_id": {
      "type": "string",
      "description": "Original request ID from the decision bundle",
      "format": "uuid"
    },
    "timestamp": {
      "type": "string",
      "description": "ISO 8601 timestamp of the original decision",
      "format": "date-time"
    },
    "input": {
      "type": "object",
      "description": "The original case input that was evaluated"
    },
    "critic_outputs": {
      "type": "array",
      "description": "Outputs from all critics for this precedent",
      "items": {
        "$ref": "critic_bundle.schema.json"
      }
    },
    "final_decision": {
      "type": "object",
      "description": "The final aggregated decision for this precedent",
      "required": ["overall_verdict"],
      "properties": {
        "overall_verdict": {
          "type": "string",
          "enum": ["ALLOW", "DENY", "REVIEW", "BLOCK"]
        },
        "reason": {
          "type": "string"
        },
        "avg_confidence": {
          "type": "number",
          "minimum": 0.0,
          "maximum": 1.0
        }
      }
    },
    "precedent_refs": {
      "type": "array",
      "description": "References to precedents that were referenced when making this decision",
      "items": {
        "type": "object"
      }
    }
  }
}
