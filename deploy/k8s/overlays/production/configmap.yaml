apiVersion: v1
kind: ConfigMap
metadata:
  name: eje-config
  labels:
    app: eje
    environment: production
data:
  APP_ENV: "production"
  LOG_LEVEL: "warning"
  ENABLE_BETA_FEATURES: "false"
  ROLLBACK_WINDOW: "10"
  HEALTHCHECK_PATH: "/health"
